name: Build Windows

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci
        working-directory: loadform

      - name: Read version
        id: version
        run: echo "value=$(node -p "require('./package.json').version")" >> $env:GITHUB_OUTPUT
        working-directory: loadform

      - name: Build frontend
        run: npm run build
        working-directory: loadform

      - name: Build Tauri
        run: npm run tauri build
        working-directory: loadform

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: loadform-windows-v${{ steps.version.outputs.value }}
          path: loadform/src-tauri/target/release/bundle/nsis/*.exe
