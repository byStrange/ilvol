{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "planet",
  "description": "Capability for dynamically created planet windows in the orbital widget",
  "windows": ["planet-*"],
  "permissions": ["core:default"]
}